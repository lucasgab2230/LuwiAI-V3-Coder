#!/bin/bash

# Example script to demonstrate training the Small Language Model

# Run the training script
# You can pass command-line arguments to slm_project/train.py if needed
# For now, assuming the script uses default configuration or reads from a config file
python3 /slm_project/train.py
